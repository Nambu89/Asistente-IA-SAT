[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "svania"
version = "1.0.0"
description = "Asistente técnico especializado en productos del Grupo SWAN"
requires-python = ">=3.8"

[tool.pytest.ini_options]
pythonpath = [
  "."
]